# Optional: override API origin. In dev, defaults to http://127.0.0.1:8000/api (browser → FastAPI, CORS-enabled).
# Production builds should set this to your deployed API base URL (must include /api).
VITE_API_BASE_URL=http://127.0.0.1:8000/api
